1) learn if elaase and else if plz in details man
2)/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* productExceptSelf(int* nums, int numsSize, int* returnSize) {
    int left[numsSize];left[0]=1;
    int right[numsSize];right[numsSize-1]=1;int j = numsSize-1;
    int* answer =(int*) malloc(numsSize * sizeof(int));         //dono why beacuse they told to?
    for(int i=1;i<numsSize;i++){
        left[i]=left[i-1]*nums[i-1];
        right[j-i]=right[j-i+1]*nums[j-i+1];
    }
    for(int i=0;i<numsSize;i++){
        answer[i]=left[i]*right[i];
        
    }
    *returnSize = numsSize; // i gess this is the only way to return an array?
    return answer;
}
learn why this format and why malloced?
3) understand break #34 leetcode
4) plz do 125 again man you suck in aray get a new barin
5) learn bit manupulation plz yo (did few & is digital logic one and gate eg 7&8=0 is true)
6) i used hash maps in python need to make my own in c and redo that questiobn litrtally using lasnguage as a tool 
7) need to code and gess save locally few code for sorting forme in c sicse i skip that and do python insted lol? crazy thing to say feel proud ngl
8)learn binary search dumbass well deeep nice ly plz man proper format plz
9)18 aug 10:57 pm take a break man leave it for a while this kind of progress will lead you no ware undretand and come after a break man
10) learn bitmask plz bro
11)Floyd Cycle detection algorithm
12)happy numbers (done ez prime at even even at odd kon ulta)
13)please understand strings do or die man this time
14)slover striver dsa sheet and no gpt less gpt and choose new language plus puthon ds learn start
15) learn 🐢🐇 Floyd’s Cycle Detection
